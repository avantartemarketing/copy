# copy to .env for local runs; never commit .env
ANTHROPIC_API_KEY=
NOTION_TOKEN=
NOTION_DATABASE_ID=
# the plan database's id (open the plan as its own page: the 32 characters before ?v=), or its whole address
APP_PASSWORD=
CLAUDE_MODEL=claude-opus-5
# only if the comms database names these differently
# NOTION_PROP_CAMPAIGN=Campaign text
# NOTION_PROP_CHANNEL=Channel Name
# NOTION_PROP_COPY=Copy
