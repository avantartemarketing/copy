{#
  COMING SOON · IG Main · Post   (the artist-introduction post; the carousel imagery is bespoke, the caption is not)
  Skeleton, from 32 real posts: artist_bio · teaser line · CTA · hashtag
  Menus under context.social.coming_soon: teaser 1-5 · cta 1-4
#}
{% set s = context.social.coming_soon if context.social and context.social.coming_soon else {} %}
{% set t = s.teaser if s.teaser is defined else 1 %}
{% set c = s.cta if s.cta is defined else 1 %}
{% set handle = artist.handle if artist.handle is defined else "" %}
{% set bio = context.artist_bio if context.artist_bio is defined else "" %}
{% set send = release.coming_soon_at if release.coming_soon_at is defined and release.coming_soon_at else release.launch_at %}
[CHANNEL] IG Main · Post, Coming Soon
[INTERNAL NAME] {{ send|ddmmyy }} · {{ release.campaign_code }} · Coming Soon · IG Main
[CAPTION]
{{ bio|tag_first(artist.name, handle) }}

{% if t == 1 %}
Our {{ artist.collaboration_ordinal }} collaboration is on the horizon.
{% elif t == 2 %}
We've been working on something new.
{% elif t == 3 %}
Our {{ artist.collaboration_ordinal }} {{ edition.unit }} collaboration is on the horizon.
{% elif t == 4 %}
On {{ release.launch_at|ukdate }}, we'll release {{ artworks|titles_q }} by {{ ("@" ~ handle) if handle else artist.name }} as a {{ release.window }} limited edition.
{% else %}
{{ artist.name }}'s {{ artist.collaboration_ordinal }} collaboration with Avant Arte arrives this {{ release.launch_at|month }}.
{% endif %}

{% if c == 1 %}
Head to our link in bio to get updates.
{% elif c == 2 %}
Link in bio to register for updates.
{% elif c == 3 %}
Link in bio to get updates.
{% else %}
Head to our link in bio for updates.
{% endif %}
{% if artist.hashtag and (s.hashtag if s.hashtag is defined else false) %}

#{{ artist.hashtag }}
{% endif %}
